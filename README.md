# KAZrag

**KAZrag** — это современная поисковая система с элементами RAG (Retrieval-Augmented Generation), предназначенная для индексации документов и семантического поиска по ним.

## ✨ Основные возможности

- **🔄 Универсальная обработка документов** — автоматическая конвертация PDF, DOCX, PPTX, XLSX, изображений и других форматов в текст
- **🧠 Умный семантический поиск** — поиск по смыслу, а не только по ключевым словам
- **📄 Распознавание текста (OCR)** — извлечение текста из изображений и сканов с поддержкой русского языка
- **🎯 Reranker** — улучшение релевантности результатов поиска
- **⚡ Гибкие настройки чанкинга** — различные стратегии разбиения документов на фрагменты
- **🌐 Веб-интерфейс** — удобная панель управления для поиска и настройки системы
- **🔐 Защита доступа** — возможность настройки API-ключей для администрирования

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонируйте репозиторий
git clone https://github.com/Neuro-Storm/KAZrag.git
cd KAZrag

# Создайте виртуальное окружение
python -m venv venv

# Активация виртуального окружения:
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка

Скопируйте файл настроек:

```bash
cp .env.example .env
```

При необходимости отредактируйте `.env` файл для настройки:
- `KAZRAG_HOST=0.0.0.0` — для доступа из локальной сети
- `KAZRAG_PORT=8000` — порт приложения
- `ADMIN_API_KEY=ваш-ключ` — для защиты админки

### 3. Запуск Qdrant

Скачайте и запустите Qdrant:
```bash
# Используйте Docker (рекомендуется для всех платформ):
docker run -p 6333:6333 -p 6334:6334 -v ./qdrant_data:/qdrant/storage:z qdrant/qdrant

# Или скачайте бинарный файл:
# Windows: qdrant.exe
# Linux: qdrant (из релизов на GitHub)
# macOS: qdrant (из релизов на GitHub)
```

### 4. Запуск приложения

```bash
python main.py
```

Приложение будет доступно по адресу:
- **Основной интерфейс:** http://localhost:8000
- **Административная панель:** http://localhost:8000/settings
- **API документация:** http://localhost:8000/docs

## 📖 Как использовать

### 1. Подготовка документов

Поместите ваши документы в папку `pdfs_to_process/`. Поддерживаемые форматы:
- **PDF** (с распознаванием текста и таблиц)
- **Документы:** DOCX, TXT, HTML, MD
- **Изображения:** JPG, PNG, TIFF (с OCR)
- **Презентации:** PPT, PPTX
- **Таблицы:** XLSX, XLS

### 2. Индексация

1. Откройте административную панель: http://localhost:8000/settings
2. Перейдите на вкладку "Индексация"
3. Укажите папку с документами
4. Нажмите "Запустить индексацию"

Процесс индексации автоматически:
- Конвертирует документы в текстовый формат
- Разбивает текст на фрагменты (чанки)
- Создает векторные представления
- Сохраняет в базу данных

### 3. Поиск

1. Откройте основной интерфейс: http://localhost:8000
2. Введите поисковый запрос на естественном языке
3. Используйте фильтры для уточнения поиска
4. Получите релевантные фрагменты документов

## 🎛️ Особенности системы

### Умный поиск
- Понимает смысл запросов, а не только ключевые слова
- Находит релевантные документы даже при неточном совпадении слов
- Учитывает контекст и семантику

### Гибкая настройка
- Выбор моделей эмбеддингов для разных задач
- Настройка стратегии разбиения документов
- Поддержка различных языков и форматов

### Веб-интерфейс
- Удобный поиск с расширенными фильтрами
- Мониторинг процесса индексации
- Управление коллекциями документов
- Настройка параметров системы

## 🔧 Конфигурация

Основные настройки доступны через веб-интерфейс в административной панели:

- **Модели эмбеддингов** — выбор модели для создания векторных представлений
- **Параметры чанкинга** — настройка размера и стратегии разбиения документов
- **Reranker** — улучшение качества поиска
- **OCR настройки** — распознавание текста из изображений
- **Фильтры поиска** — настройка метаданных для точной фильтрации

## 📚 Технологии

- **FastAPI** — веб-фреймворк
- **Qdrant** — векторная база данных
- **Docling** — обработка документов
- **EasyOCR** — распознавание текста
- **HuggingFace Transformers** — модели эмбеддингов

## 📝 Лицензия

Проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE).
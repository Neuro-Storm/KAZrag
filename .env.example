# Пример файла конфигурации переменных окружения
# Скопируйте этот файл в .env и настройте под себя

# Разрешенные источники для CORS (через запятую)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# API ключ для админки (оставьте пустым для отключения)
ADMIN_API_KEY=your-secret-api-key-here

# Директория для кэша моделей fastembed (BM25 и других sparse моделей)
# FASTEMBED_CACHE_DIR=./models/fastembed_cache

# Примечание: Для обработки файлов различных форматов могут потребоваться дополнительные зависимости:
# - Для DJVU файлов: требуется установить DjVuLibre (ddjvu tool)
# - Для DOCX файлов: python-docx (устанавливается автоматически)
# - Для HTML файлов: html2text (устанавливается автоматически)
# - Для презентаций (PPT/PPTX): python-pptx (устанавливается автоматически)
# - Для таблиц (XLSX/XLS): pandas и openpyxl (устанавливаются автоматически)